{
  "name": "spa-benchmark-suite",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Benchmark analysis utilities for SPA benchmark suite.",
  "scripts": {
    "results:aggregate": "node results/analysis/aggregate.js",
    "results:charts": "node results/analysis/charts.js",
    "results:report": "node results/analysis/report.js",
    "results:build": "npm run results:aggregate && npm run results:charts && npm run results:report"
  },
  "dependencies": {
    "chart.js": "^4.4.4",
    "chartjs-node-canvas": "^4.3.0",
    "csv-parse": "^5.5.2",
    "markdown-table": "^3.0.3"
  }
}
  "scripts": {
    "install:apps": "npm --prefix react-app install && npm --prefix vue-app install && (test -d svelte-app && npm --prefix svelte-app install || true)",
    "bench:react": "node scripts/run.js --tech react",
    "bench:vue": "node scripts/run.js --tech vue",
    "bench:svelte": "node scripts/run.js --tech svelte",
    "bench:all": "node scripts/run.js"
  },
  "dependencies": {
    "chrome-launcher": "^0.15.3",
    "lighthouse": "^11.7.1",
    "playwright": "^1.43.0"
  }
}

